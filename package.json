{
  "name": "usermap-root",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/server",
    "apps/desktop"
  ],
  "scripts": {
    "dev": "npm run dev:server & npm run dev:desktop",
    "dev:server": "npm run dev --workspace=apps/server",
    "dev:desktop": "npm run dev --workspace=apps/desktop",
    "build": "npm run build --workspace=apps/server && npm run build --workspace=apps/desktop",
    "lint": "npm run lint --workspace=apps/server && npm run lint --workspace=apps/desktop"
  }
}
